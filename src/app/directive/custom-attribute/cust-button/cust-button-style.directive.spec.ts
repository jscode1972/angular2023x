import { CustButtonStyleDirective } from './cust-button-style.directive';

describe('CustButtonStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustButtonStyleDirective();
    expect(directive).toBeTruthy();
  });
});
