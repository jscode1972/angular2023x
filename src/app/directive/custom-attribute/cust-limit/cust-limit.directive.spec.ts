import { CustLimitDirective } from './cust-limit.directive';

describe('CustLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new CustLimitDirective();
    expect(directive).toBeTruthy();
  });
});
