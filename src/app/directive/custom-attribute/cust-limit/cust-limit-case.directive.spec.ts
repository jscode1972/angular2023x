import { CustLimitCaseDirective } from './cust-limit-case.directive';

describe('CustLimitCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CustLimitCaseDirective();
    expect(directive).toBeTruthy();
  });
});
