import { CustButtonConfirmDirective } from './cust-button-confirm.directive';

describe('CustButtonConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new CustButtonConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
